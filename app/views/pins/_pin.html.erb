<div id="<%= dom_id pin %>">
  <p>
    <strong>Title:</strong>
    <%= pin.title %>
  </p>

  <%= pin.body  %>
  <br />
  <%= image_tag(pin.image_as_thumbnail) if pin.image.attached? %>
  <hr />
  <% pin.pictures_as_thumbnails.each do |picture| %>
    <%= image_tag(picture) %>
  <% end %>

</div>
